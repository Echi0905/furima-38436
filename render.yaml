databases:
  - name: furima_38436_development
    databaseName: furima_38436_development
    user: furima furima 
    region: singapore

services:
  - type: web
    name: furima_38436_development
    env: ruby
    region: singapore
    plan: free
    buildCommand: "./bin/render-build.sh"
    startCommand: "bundle exec puma -C config/puma.rb"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: rails7_sample_staging
          property: connectionString
      - key: RAILS_MASTER_KEY
        sync: false